<Window x:Class="TreeCadN.kommunikacii.kommunikacii_main"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TreeCadN.kommunikacii"
        mc:Ignorable="d"
        Title="kommunikacii" Height="462" Width="878.425" WindowStartupLocation="CenterScreen">
    <Grid>
        <TreeView x:Name="tv1" HorizontalAlignment="Left"  Height="412" Margin="10,10,0,0" VerticalAlignment="Top" Width="266" SelectedItemChanged="tv1_SelectedItemChanged">
            <TreeView.ItemTemplate>
                <HierarchicalDataTemplate DataType="{x:Type local:Models3d}" ItemsSource="{Binding Items}" >
                    <TextBlock Text="{Binding name}" MinWidth="150"/>
                </HierarchicalDataTemplate>
            </TreeView.ItemTemplate>

        </TreeView>
        <ListBox  x:Name="lb1" HorizontalAlignment="Left" Height="336" VerticalAlignment="Top" Width="579" Margin="281,10,0,0" SelectionChanged="lb1_SelectionChanged" ScrollViewer.HorizontalScrollBarVisibility="Disabled">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Vertical" Width="150">
                        <Image Width="150" Height="150" Source="{Binding Path=jpg_path}"  />
                        <TextBlock Text="{Binding Path=name}"/>
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
            <ListBox.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel/>
                    </ItemsPanelTemplate>
            </ListBox.ItemsPanel>
        </ListBox>
        <Button Content="отмена" HorizontalAlignment="Left" Height="22" Margin="788,400,0,0" VerticalAlignment="Top" Width="74" Click="Button_Click_2"/>
        <Button Content="ок" HorizontalAlignment="Left" Margin="697,400,0,0" VerticalAlignment="Top" Width="75" Click="Button_Click_1"/>
        <Button Content="Вертик/гориз" HorizontalAlignment="Left" Margin="295,363,0,0" VerticalAlignment="Top" Width="64" Height="59" RenderTransformOrigin="-0.547,3.229"/>
        <Button Content="Добавить 3ds обьект" HorizontalAlignment="Left" Margin="364,400,0,0" VerticalAlignment="Top" Width="130" Click="Button_Click"/>

    </Grid>
</Window>
